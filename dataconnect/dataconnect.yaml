specVersion: "v1beta"
serviceId: "stock-simulator-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "stock-simulator-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
