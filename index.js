import 'react-native-gesture-handler'; // Must be the first import
import { registerRootComponent } from 'expo';
import App from './App';

// Register the main component correctly
registerRootComponent(App);